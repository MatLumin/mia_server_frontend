<script>

let {owner_surname,phone_number,state_of_now,flood_wait_value,message_portion,group_portion,current_message_id,current_group_username,sending_report} = $props();

</script>


<div class="general_border secondry_divs_colors general_padding_and_margin agent_card">
owner_surname = {owner_surname}
phone_number = {phone_number}
state_of_now = {state_of_now}
flood_wait_value = {flood_wait_value}
message_portion = {message_portion}
group_portion = {group_portion}
current_message_id = {current_message_id}
current_group_username = {current_group_username}
sending_report  = {sending_report}
</div>